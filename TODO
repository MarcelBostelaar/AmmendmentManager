Make sure the folder with the primary git directorie(s) is a volume, but temporary folders are party of the docker container, they dont need to be saved.

nodemon --exec 'node --inspect=0.0.0.0:9229 --require ts-node/register src/index.ts'